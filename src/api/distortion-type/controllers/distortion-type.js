'use strict';

/**
 * distortion-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::distortion-type.distortion-type');
